# 修复前故障复现（Docker）

## 项目与标准命令

项目使用 Go 1.26。标准验证命令为：

```bash
go test -count=20 ./...
```

## 环境构建与编译

本地 `go build ./...` 可以通过，故障出现在 HTTP 登记请求的请求体生命周期检查中。

## 故障触发步骤

执行标准验证命令，发送一条有效的登记请求并检查请求体是否被关闭。

## 实际错误输出

```text
request body was not closed
```

## 期望行为

登记、批量登记、审核和放行请求处理结束后，都应释放请求体资源。
